{{ asset_add('scripts.js', 'anomaly.field_type.addon::js/select2.full.min.js') }}
{{ asset_add('scripts.js', 'anomaly.field_type.addon::js/input.js') }}
{{ asset_add('styles.css', 'anomaly.field_type.addon::less/select2.less') }}
{{ asset_add('styles.css', 'anomaly.field_type.addon::less/select2-bootstrap.less') }}

<select
        class="form-control"
        name="{{ field_type.input_name }}"
        {{ field_type.disabled ? 'disabled' }}
        {{ field_type.required ? 'required' }}>

    {% if not field_type.required %}
        <option>{{ trans(field_type.placeholder) }}</option>
    {% endif %}

    {% for value, option in field_type.options %}
        <option value="{{ value }}" {{ value == field_type.value.namespace ? 'selected' }}>{{ trans(option) }}</option>
    {% endfor %}

</select>
